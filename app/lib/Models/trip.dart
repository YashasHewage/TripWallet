
class Trip {
  String country;
  DateTime startDate;
  DateTime endDate;
  double budget;

  Trip({
    required this.country,
    required this.startDate,
    required this.endDate,
    required this.budget,
  });
}